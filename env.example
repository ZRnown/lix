# DiscuzSentinel 环境变量配置示例
# 复制此文件为 .env 并填入实际值
# 命令：cp env.example .env

# ==================== 必需配置 ====================

# 目标驿站 FID 列表（逗号分隔）
DISCUZ_TARGET_FIDS=147,148

# Cookie（从浏览器 F12 获取）
# 重要：不要将 .env 文件提交到 Git！
# 获取方法：浏览器 F12 -> Network -> 查看任意请求的 Request Headers -> Cookie
DISCUZ_COOKIE=your_cookie_here

# ==================== 告警配置（强烈建议配置）====================
# 注意：必须至少配置一个 Webhook 才能收到 Cookie 过期提醒！

# 钉钉 Webhook URL（可选，但建议配置）
# 如果配置了，Cookie 过期时会自动推送到钉钉
DINGTALK_WEBHOOK=https://oapi.dingtalk.com/robot/send?access_token=YOUR_TOKEN
# 钉钉加签密钥（可选，建议开启加签）
DINGTALK_SECRET=
# 钉钉自定义关键词（可选，若开启关键词校验，消息需包含该关键词）
DINGTALK_KEYWORD=

# 飞书 Webhook URL（可选，但建议配置）
# 如果配置了，Cookie 过期时会自动推送到飞书
FEISHU_WEBHOOK=https://open.feishu.cn/open-apis/bot/v2/hook/YOUR_TOKEN

# 系统告警 Webhook（可选，用于 Cookie 失效等关键错误）
# 如果未设置，将使用 DINGTALK_WEBHOOK 或 FEISHU_WEBHOOK
# 建议：可以配置一个专门的告警 Webhook，确保重要告警不被淹没
SYSTEM_ALERT_WEBHOOK=

# ==================== 可选配置 ====================

# 图片处理模式：direct（直接链接）、text_only（仅文字）、upload_feishu（上传到飞书）
IMAGE_MODE=direct

# 日志文件路径（可选）
LOG_FILE=discuz_sentinel.log

# 日志级别：DEBUG, INFO, WARNING, ERROR（可选）
LOG_LEVEL=INFO

