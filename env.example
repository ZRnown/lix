# DiscuzSentinel 环境变量配置示例
# 复制此文件为 .env 并填入实际值
# 命令：cp env.example .env

# ==================== 必需配置 ====================

# 目标驿站 FID 列表（逗号分隔）
DISCUZ_TARGET_FIDS=147,148

# Cookie（从浏览器 F12 获取）
# 重要：不要将 .env 文件提交到 Git！
# 获取方法：浏览器 F12 -> Network -> 查看任意请求的 Request Headers -> Cookie
DISCUZ_COOKIE=your_cookie_here

# ==================== 推送与告警 ====================
# 至少配置一个渠道（Webhook 或 App 模式），否则收不到 Cookie 过期提醒

# --- 钉钉 Webhook（基础模式，图片直链可能因防盗链无法显示） ---
DINGTALK_WEBHOOK=https://oapi.dingtalk.com/robot/send?access_token=YOUR_TOKEN
DINGTALK_SECRET=
DINGTALK_KEYWORD=

# --- 钉钉 App 模式（推荐，支持原生发图；需 openConversationId） ---
DINGTALK_APP_KEY=
DINGTALK_APP_SECRET=
DINGTALK_TARGET_ID=  # 群的 openConversationId，配置后会优先走 App 模式

# --- 飞书 Webhook（基础模式，图片以链接形式呈现；告警优先发这里） ---
FEISHU_WEBHOOK=https://open.feishu.cn/open-apis/bot/v2/hook/YOUR_TOKEN

# --- 飞书 App 模式（推荐，支持原生发图；需 chat_id） ---
FEISHU_APP_ID=
FEISHU_APP_SECRET=
FEISHU_TARGET_ID=      # 群聊 chat_id（如 oc_xxx），配置后会优先走 App 模式

# --- 系统告警 Webhook（可选；若不填则默认用上面的 FEISHU_WEBHOOK 发告警） ---
SYSTEM_ALERT_WEBHOOK=

# ==================== 可选配置 ====================

# 图片处理模式：direct（直接链接）、text_only（仅文字）、upload_feishu（上传到飞书）
IMAGE_MODE=direct

# 文本预览长度（0 或负数不截断）
PREVIEW_LIMIT=4000

# 日志文件路径与级别
LOG_FILE=discuz_sentinel.log
LOG_LEVEL=INFO
# 日志按天轮转保留天数（0 表示不轮转）
LOG_RETENTION_DAYS=7

